<h1>All Categories</h1>
<% if @categories %>
  <ul class="list">
    <% @categories.each do |category| %>
      <li class="list-item"><%= link_to category.name, category_path(category) %></li>
    <% end -%>
  </ul>
<% else %>
  <p>No categories found</p>
<% end -%>
